# Site images

Drop the two hero screenshots here so the page shows them:

- `forge-overview.png`: the Forge editor with the sample scene loaded and an
  object selected. The same shot used at the top of the Forge README.
- `aurora-overview.png`: the powered Aurora Station interior with the planet
  through the window. The same shot used at the top of the Aurora README.

A wide 16:9 crop around 1600x900 looks best in the layout.
